set-option -g prefix `
# set-option - prefix C-a

# I need VIM to respond faster.
set -s escape-time 0

unbind-key C-b
bind-key C-a last-window
bind-key ` last-window
bind-key a send-prefix

set -g base-index 1
