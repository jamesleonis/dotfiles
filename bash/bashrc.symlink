# James' bashrc file

export CLICOLOR=1

# MMM, Delicious VIM in my terminal. Have some!
set -o vi

# I'm easy like that.
PS1='\$ '

# Add my own personal executable directory
export PATH="$HOME/.bin:$PATH"
